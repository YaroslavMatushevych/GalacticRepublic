import React from 'react';
import './ShipPage.module.css';

interface Props {
  text: string;
  onClick?: () => {};
}

const ShipPage: React.FC<Props> = ({ text, onClick }) => {
  return (
    <>
    </>
  )
}

export default ShipPage;