import React, { memo } from 'react';
import './ShipPage.css';

interface Props {
  text: string;
  onClick?: () => {};
}

const ShipPage: React.FC<Props> = ({ text, onClick }) => {
  return (
    <>
    </>
  )
}

export default ShipPage;